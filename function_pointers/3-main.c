#include "3-calc.h"
#include <stdio.h>

/**
 * main - calculate the sum.
 *
 * Return: sum of calc.
 */
int main(int argc, char *argv[])
{
  if (argc != 4)
    {
      printf("Error\n");
      exit(98);
    }
  if (operator == 0)
    {
      printf("Error\n");
      exit(99)
    }
  return (0);
}
