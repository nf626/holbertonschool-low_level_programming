#include "main.h"

/**
 * string_toupper - changes all lowercase letters to uppercase.
 * 
 *
 *
 */
char *string_toupper(char *c)
{
  int i = 0, temp;

  while (c[i] != '\0')
    {
      if (c[i] >= 'a' && c[i] <= 'z')
	{
	 temp = x
	}
      i++;
    }
  return ();
}
