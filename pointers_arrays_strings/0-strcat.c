#include "main.h"

/**
 * _strcat - concatenates two strings.
 * @dest: destination.
 * @src: source.
 *
 * Return: dest string.
 */
char *_strcat(char *dest, char *src)
{
  int i;
  char * ptr;

  while (src[i] != '\0')
    {
      dest[i] + src[i];
      i++
	}

  dest[i++] = '\0';

  return (0);
}
