#include<stdio.h>
#include <stdlib.h>

/**
 * main - Prints alphabet in lowercase except for q and e.
 *
 * Return: Success
 *
 */
int main(void)
{
char n;

for (n = '0'; n <= '9'; n++)
{
putchar(n);
}

putchar('\n');
return (0);
}
