#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _abs - computes the absolute value of an integer.
 * @r: outputs the absolute value.
 *
 * Return: Always 0.
 */
int _abs(int r)
{
if (r >= 0)
{
return (r);
}
else
{
return (-r);
}

}
